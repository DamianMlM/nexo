---
import { Store, CreditCard, Package, Users } from 'lucide-react';
---

<section class="relative pt-32 pb-20 md:pt-48 md:pb-32 overflow-hidden flex flex-col items-center text-center">
  <!-- Mesh Gradient Background -->
  <div class="absolute inset-0 -z-10 bg-slate-950">
    <div class="absolute top-0 left-1/2 -translate-x-1/2 w-[800px] h-[600px] bg-blue-600/20 rounded-full blur-[100px] animate-pulse"></div>
    <div class="absolute bottom-0 right-0 w-[600px] h-[600px] bg-cyan-500/10 rounded-full blur-[120px]"></div>
    <div class="absolute inset-0 bg-[url('/grid.svg')] bg-center [mask-image:linear-gradient(180deg,white,rgba(255,255,255,0))] opacity-20"></div>
  </div>

  <div class="relative z-10 max-w-4xl px-4 mx-auto">
    <div class="inline-flex items-center gap-2 px-3 py-1 mb-6 text-xs font-semibold text-cyan-300 uppercase tracking-wider bg-cyan-500/10 border border-cyan-500/20 rounded-full">
      <span class="w-2 h-2 rounded-full bg-cyan-400 animate-ping"></span>
      Nueva Plataforma 2026
    </div>
    
    <h1 class="text-5xl md:text-7xl font-black text-white tracking-tight leading-tight mb-6">
      Tu Negocio, <br/>
      Tu Tienda, <br/>
      <span class="text-transparent bg-clip-text bg-gradient-to-r from-cyan-400 via-blue-500 to-purple-600">
        Tu Control.
      </span>
    </h1>
    
    <p class="text-lg md:text-xl text-slate-400 max-w-2xl mx-auto mb-16 leading-relaxed">
      La plataforma todo-en-uno que te da tu propia web de pedidos personalizada y el sistema de gestión para operarla.
    </p>

    <!-- Visual Abstracto: Icon Network -->
    <div class="relative w-full max-w-lg mx-auto h-[300px] md:h-[350px] flex items-center justify-center">
      <!-- Connection Lines -->
      <div class="absolute inset-0 flex items-center justify-center">
        <!-- Lines radiating from center -->
        <div class="w-[2px] h-[140px] bg-gradient-to-b from-blue-500/50 to-transparent absolute top-1/2 -translate-y-full origin-bottom rotate-0"></div> <!-- Top -->
        <div class="w-[2px] h-[140px] bg-gradient-to-b from-blue-500/50 to-transparent absolute top-1/2 -translate-y-full origin-bottom rotate-120 transform translate-x-[60px] translate-y-[35px]"></div> <!-- Right Down -->
        <div class="w-[2px] h-[140px] bg-gradient-to-b from-blue-500/50 to-transparent absolute top-1/2 -translate-y-full origin-bottom -rotate-120 transform -translate-x-[60px] translate-y-[35px]"></div> <!-- Left Down -->
      </div>
      
      <!-- Central Hub -->
      <div class="relative z-20 w-24 h-24 bg-slate-900 border border-blue-500/50 rounded-2xl shadow-[0_0_50px_rgba(59,130,246,0.3)] flex items-center justify-center">
        <Store className="w-12 h-12 text-blue-400" />
      </div>

      <!-- Satellites -->
      
      <!-- Top: Sales -->
      <div class="absolute top-10 left-1/2 -translate-x-1/2 z-20 w-14 h-14 bg-slate-900 border border-cyan-500/30 rounded-xl flex items-center justify-center animate-bounce duration-[3000ms]">
        <CreditCard className="w-6 h-6 text-cyan-400" />
      </div>

      <!-- Right: Inventory -->
      <div class="absolute bottom-16 right-10 md:right-20 z-20 w-14 h-14 bg-slate-900 border border-purple-500/30 rounded-xl flex items-center justify-center animate-bounce duration-[4000ms]">
        <Package className="w-6 h-6 text-purple-400" />
      </div>

      <!-- Left: Customers -->
      <div class="absolute bottom-16 left-10 md:left-20 z-20 w-14 h-14 bg-slate-900 border border-emerald-500/30 rounded-xl flex items-center justify-center animate-bounce duration-[3500ms]">
        <Users className="w-6 h-6 text-emerald-400" />
      </div>

      <!-- Glow Underlay -->
      <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-48 h-48 bg-blue-500/20 rounded-full blur-3xl -z-10"></div>
    </div>
  </div>
</section>
